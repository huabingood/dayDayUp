package MyBean

class Test3Class {
   var sum:Int = 0;

  /*def getSum()  {
    return sum; // scala中如果没有return语句，默认返回最后一次计算的结果。但是这里没有运算啊！！！
  }
  def setSum(sum:Int): Unit ={
    this.sum = sum;
  }

  // 测试无return返回的情况
  def f()="this is test of return;";
  // 当声明一个无返回值的函数时*/

}
